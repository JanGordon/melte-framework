
{
// script for out-hi116.js
 const SELF = document.querySelector("[melte-id='hi116']")
    //whereas this is run at build time and can be accesed in html
let count =counthi116

    //this keeps the state of the variable

    SELF.querySelector("button").addEventListener("click", function() {
        console.log(SELF.querySelector("button").innerText)
        count += 1
        SELF.querySelector("#s").innerText = (count).toString()
    })
    //SELF.querySelector("button").innerText = (count).toString()
    //Using js to update very page load is too slow and causes an update which is very ugly

}{
// script for out-hi216.js
 const SELF = document.querySelector("[melte-id='hi216']")
    //whereas this is run at build time and can be accesed in html
let count =counthi216

    //this keeps the state of the variable

    SELF.querySelector("button").addEventListener("click", function() {
        console.log(SELF.querySelector("button").innerText)
        count += 1
        SELF.querySelector("#s").innerText = (count).toString()
    })
    //SELF.querySelector("button").innerText = (count).toString()
    //Using js to update very page load is too slow and causes an update which is very ugly

}{
// script for out-hi316.js
 const SELF = document.querySelector("[melte-id='hi316']")
    //whereas this is run at build time and can be accesed in html
let count =counthi316

    //this keeps the state of the variable

    SELF.querySelector("button").addEventListener("click", function() {
        console.log(SELF.querySelector("button").innerText)
        count += 1
        SELF.querySelector("#s").innerText = (count).toString()
    })
    //SELF.querySelector("button").innerText = (count).toString()
    //Using js to update very page load is too slow and causes an update which is very ugly

}{
// script for out-hi416.js
 const SELF = document.querySelector("[melte-id='hi416']")
    //whereas this is run at build time and can be accesed in html
let count =counthi416

    //this keeps the state of the variable

    SELF.querySelector("button").addEventListener("click", function() {
        console.log(SELF.querySelector("button").innerText)
        count += 1
        SELF.querySelector("#s").innerText = (count).toString()
    })
    //SELF.querySelector("button").innerText = (count).toString()
    //Using js to update very page load is too slow and causes an update which is very ugly

}